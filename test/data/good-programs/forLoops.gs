whiles (bless)
        printes "make it stop"
GollumGollum

revolves (it i = 0; i <= 100; i++)
        ifes (i % 15 == 0)
            printes "Blessed"
        GollumGollum
        ifElses (i % 5 == 0)
            printes "Smeagol"    
        GollumGollum
GollumGollum
